{
	"name": "backend",
	"version": "1.0.0",
	"description": "",
	"scripts": {
		"d-all": "concurrently \"npm:dev -C auth\" \"npm:dev -C comm\"",
		"i-all": "(npm i -C auth) && (npm i -C comm)",
		"b-all": "tsc -b -v",
		"s-all": "concurrently \"npm:start -C auth\" \"npm:start -C comm\""
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"concurrently": "^7.6.0"
	}
}
